services:
  fastapi:
    restart: unless-stopped
    image: ghcr.io/builtbyrb/fastapi-template:latest
    env_file:
      - ${ENV_FILE_NAME}
    networks:
      - redis-net
      - database-net
      - fastapi-net
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512M
    healthcheck:
      test: python3 -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/health/', timeout=5)"
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 20s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.fastapi.rule=Host(`10.66.66.1`)"
      - "traefik.http.routers.fastapi.entrypoints=web"
      - "traefik.http.services.fastapi.loadbalancer.server.port=8000"
      - "traefik.docker.network=fastapi-net"
    depends_on:
      pgbouncer:
        condition: service_healthy
      postgredb:
        condition: service_healthy
      redis:
        condition: service_healthy
