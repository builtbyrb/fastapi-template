[sources.docker_logs]
type = "docker_logs"
docker_host = "tcp://docker-proxy:2375"

[sinks.axiom]
type = "axiom"
inputs = ["docker_logs"]
token = "${AXIOM_TOKEN}"
dataset = "${AXIOM_DATASET_NAME}"
region = "${AXIOM_REGION}"

[api]
enabled = true
address = "0.0.0.0:8686"
